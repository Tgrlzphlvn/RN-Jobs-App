import {StyleSheet} from 'react-native';

export const favButtonStyles = StyleSheet.create({
  container: {
    backgroundColor: 'teal',
    borderRadius: 10,
    alignItems: 'center',
    shadowColor: 'black',
    shadowOffset: 8,
    shadowOpacity: 1,
    shadowRadius: 8,
  },
  text: {
    fontSize: 20,
    fontWeight: 'bold',
    color: 'white',
    padding: 10,
  },
});
